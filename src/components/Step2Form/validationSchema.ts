// components/Step1Form/validationSchema.ts
// components/Step2Form/validationSchema.ts
import * as yup from "yup";

const validationSchema = yup.object({
  // Add the validation rules for each field based on the requirements
});

export default validationSchema;
